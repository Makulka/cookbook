 /* global $ */
$('#results').html("<%= j (render 'user_recipes/result.html') %>");
$('#search_param').val('');

$('.message .close').on('click', function() {
  $(this).closest('.message').transition('fade');
});
